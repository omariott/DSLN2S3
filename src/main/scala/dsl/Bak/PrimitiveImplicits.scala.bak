

object PrimitiveImplicits{


  case class Neurons(num: Int)

  implicit class IntImplicits(l: Int) {
    def neurons: Neurons = {
      if(l <= 0) throw new IllegalArgumentException("There must be at least one neuron.")
      Neurons(l)
    }
  }
}